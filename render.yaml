services:
  - type: web
    name: diyar-ai-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: PORT
        value: 8000
      - key: ADMIN_TOKEN
        generateValue: true
    healthCheckPath: /health
